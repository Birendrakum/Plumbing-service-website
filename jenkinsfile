pipeline {
    agent any

    stages {
        stage('Run Script') {
            steps {
                echo 'Executing single-stage pipeline...'
                sh 'echo "Hello from Jenkins!"'
            }
        }
    }
}
